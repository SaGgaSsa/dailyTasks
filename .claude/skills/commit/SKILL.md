# Commit Skill

1. Run `npm run build` and fix any errors

2. Run `git diff --staged` to review changes

3. Write a concise commit message in English (imperative mood)

4. Run `git add -A && git commit -m "<message>"`

5. Show the commit hash and summary
